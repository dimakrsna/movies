import styled from 'styled-components'

export const Button = styled.span`
  display: inline-block;
  height: 30px;
  margin-right: 10px;
  line-height: 30px;
  padding: 0 10px;
  margin-top: 10px;
  border-radius: 3px;
  background: #ccc;
  cursor: pointer;
  transition: 0.2s;

  &:hover {
    background: #a6a2a2;
  }
`